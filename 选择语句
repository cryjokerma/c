#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int main()
{
	int input = 0;//input 作为输入值

	printf("是否向前？（1/0）\n");
	scanf("%d", &input);
	if (input == 1)//==用来判断
	{
		printf("向前");
	}
	else
	{
		printf("不向前");
	}
	return 0;
}
