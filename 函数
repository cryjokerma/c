#define  _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
//int sum = x + y;//用函数方式实现如下
Add(int a, int b)

{
	int z = a + b;
	return z;
}
int main()
{
	int num1;
	int num2;
	
	scanf("%d%d", &num1, &num2);
	int sum2=Add(num1, num2);
	printf("%d", sum2);
	return 0;
}
//同一工程可以调用不同项目中的函数
